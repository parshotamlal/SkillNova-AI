{
  "name": "skillnova-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "cd frontend && npm install && npm run build",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm start --prefix backend"
  }
}